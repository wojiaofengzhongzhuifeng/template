# ================================
# 数据库配置
# ================================

# PostgreSQL 配置（docker-compose 和应用共用）
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres123"
POSTGRES_DB="app-01"

# 数据库连接字符串
# 本地开发: localhost | Docker部署: postgres
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/app-01"

# ================================
# 应用配置
# ================================

# 应用基础 URL
# 本地开发: http://localhost:3000 | Docker部署: http://服务器IP:3000
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# API 路径前缀
NEXT_PUBLIC_API_PATH="/api"

# ================================
# Redis 配置
# ================================

# 本地开发: localhost | Docker部署: redis
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# ================================
# 邮件配置（SMTP）
# ================================

SMTP_HOST="smtp.qq.com"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER=""
SMTP_PASS=""

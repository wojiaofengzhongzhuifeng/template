/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
        '@tailwindcss/postcss': {},
        'postcss-nesting': {},
        'postcss-mixins': {},
    },
};

export default config;
